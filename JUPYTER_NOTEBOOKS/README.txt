This folder contains the code that was used to train the model,
as well as compute the avg bike data for the right hand siide bar.

Neither of these scripts are necessary for the running of the web application.